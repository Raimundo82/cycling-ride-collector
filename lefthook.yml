pre-commit:
  parallel: true
  commands:
    format:
      run: make format
    check-format:
      run: make check-format
    lint:
      run: make lint
    test:
      run: make test

pre-push:
  parallel: true
  commands:
    checks:
      run: make prepush
