pre-commit:
  commands:
    format:
      run: make format
      stage_fixed: true
    check-format:
      run: make check-format
    lint:
      run: make lint
    test:
      run: make test

pre-push:
  parallel: true
  commands:
    checks:
      run: make prepush
